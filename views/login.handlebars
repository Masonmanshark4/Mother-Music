
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign Up Page</title>
    <style>
        /* Reset default margin and padding */
        body, ul {
            margin: 0;
            padding: 0;
        }

        /* Style the navigation bar */
        nav {
            background-color: #273F59;
            color: white;
            text-align: center;
            padding: 10px 0;
        }

        /* Style the navigation links */
        .nav-links {
            list-style-type: none;
            padding: 0;
            margin: 0;
        }

        .nav-links li {
            display: inline-block;
            margin-right: 20px;
        }

        .nav-links li a {
            color: white;
            text-decoration: none;
            font-weight: bold;
            padding: 10px;
        }

        .nav-links li a:hover {
            background-color: #EBDFC7;
        }

        /* Flex container for input groups */
        .input-group {
            display: flex;
            align-items: center;
            margin-bottom: 10px; /* Add some space between input groups */
        }

        /* Style for labels */
        .user-texts {
            font-family: Arial, Helvetica, sans-serif;
            color: #273F59;
            padding: 8px;
            margin-right: 10px; /* Add space between label and input */
            flex: 0 0 150px; /* Set fixed width for labels */
        }

        /* Style for text input boxes */
        .user-texts[type="text"],
        .user-texts[type="password"] {
            flex: 1; /* Grow to fill available space */
            padding: 8px;
            border: 1px solid #EBDFC7;
            border-radius: 5px;
        }

        /* Style for the submit button */
        .login-signup-btn {
            background-color: #273F59;
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

        /* Change the background color of the submit button on hover */
        .login-signup-btn:hover {
            background-color: #EBDFC7;
        }

        /* Style the form sections */
        .form-section {
            background-color: #f0f0f0;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        } 

        /* Flex container for signup/login links */
        .signup-login-links {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

    </style>
</head>
<section class="login-page">
    <div class="form-heading">
        <h1 class="h1-text">Log In</h1>
          <style>
            .h1-text {
                font-family: Arial, Helvetica, sans-serif;
                color: #273F59;
                padding-left: 20px;
            }
        </style>
    </div>
    <div class="form-section">
        <form class="login-form">
            <div>
           <div class="input-group">
                <label for="username-login" class="user-texts">Select a Username:</label>
                <input type="text" class="user-texts" id="username-signup" />
            </div>
            <div class="input-group">
                <label for="password-login" class="user-texts">Select a password:</label>
                <input type="password" class="user-texts" id="password-signup" />
            </div>
            <div>
                <button type="submit" class="login-signup-btn">Log In!</button>
            </div>
        </form>
    </div>

    <a href="/signup">
                    <h4 class="user-texts">Sign up Instead</h4>
                  </a>
</section>

<script src="/Pulic/js/login.js"></script>